name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build
        env:
          NODE_ENV: production
          BASE_PATH: '/autoarchivepro'
      
      - name: Verify build output
        run: |
          echo "=== 验证构建输出 ==="
          echo "检查步骤名称..."
          echo "--- 所有步骤标题 ---"
          grep -o 'ant-steps-item-title">[^<]*' out/index.html || echo "未找到步骤"
          echo ""
          echo "--- 验证'选择分类' ---"
          if grep -q "选择分类" out/index.html; then
            echo "✓ 找到'选择分类' - 构建正确"
            COUNT=$(grep -o "选择分类" out/index.html | wc -l)
            echo "出现次数: $COUNT"
          else
            echo "✗ 错误: 未找到'选择分类'"
            echo "实际内容:"
            grep -o 'ant-steps-item-title">[^<]*' out/index.html || echo "无"
            exit 1
          fi
          echo ""
          echo "--- 检查旧版本 ---"
          if grep -q "一级分类\|二级分类" out/index.html; then
            echo "✗ 错误: 发现旧版本步骤名称"
            grep -o "一级分类\|二级分类" out/index.html
            exit 1
          else
            echo "✓ 未发现旧版本步骤名称"
          fi
          echo "=== 验证完成 ==="

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: './out'

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4

